import { DisplayPermissionDirective } from './display-permission.directive';

describe('DisplayPermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayPermissionDirective();
    expect(directive).toBeTruthy();
  });
});
