import { LinkPermissionDirective } from './link-permission.directive';

describe('LinkPermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new LinkPermissionDirective();
    expect(directive).toBeTruthy();
  });
});
