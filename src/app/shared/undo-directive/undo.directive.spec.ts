import { UndoDirective } from './undo.directive';

describe('UndoDirective', () => {
  it('should create an instance', () => {
    const directive = new UndoDirective();
    expect(directive).toBeTruthy();
  });
});
